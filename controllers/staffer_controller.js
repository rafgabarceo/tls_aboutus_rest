import { StafferService } from "../services/staffer_service";
import { DBConnection } from "../classes/db_connect.mjs";
import express from "express";

class StafferController {

    // Will handle GET. Returns all staffers
    static staffer_list = (req, res) => {

    };

    // Will handle GET. Returns specific staffer.
    static staffer_details = (req, res) => {

    };

    // Will handle POST. Creates a new staffer row. 
    static staffer_create = (req, res) => {

    };

    // Will handle DELETE. Deletes a staffer. 
    static staffer_delete = (req, res) => {

    };

    // Will handle PUT. Updates a staffer based on their id and argument. 
    static staffer_update = (req, res) => {

    };
};

export {StafferController};